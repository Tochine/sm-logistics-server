const config = require("../../config");
const { ServiceError } = require("../../exceptions");
const models = require("../../database/models/Index");

class Items {
  // static acceptRuest = async () => {
  //   const item = await models.
  // }
}
